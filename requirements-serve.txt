# Serving-layer dependencies (serve.py: OpenAI-compatible API + dashboard).
# The core voice agent (main.py) does NOT need these — see requirements.txt.
fastapi
uvicorn[standard]
psutil
# Voice chat (dashboard mic → Whisper STT; TTS uses macOS `say` or pyttsx3)
numpy
faster-whisper
python-multipart
# Self-signed HTTPS for phone access (--ssl / --lan); falls back to openssl CLI
cryptography
